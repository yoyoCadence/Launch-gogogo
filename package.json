{
  "name": "launch-gogogo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "build": "node --check app-core.js && node --check app.js && node --check service-worker.js",
    "test": "vitest run tests/unit tests/component",
    "test:unit": "vitest run tests/unit",
    "test:component": "vitest run tests/component",
    "test:e2e": "playwright test",
    "test:all": "npm run lint && npm run build && npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@playwright/test": "^1.52.0",
    "eslint": "^9.25.1",
    "globals": "^16.0.0",
    "http-server": "^14.1.1",
    "jsdom": "^26.1.0",
    "vitest": "^3.1.2"
  }
}
